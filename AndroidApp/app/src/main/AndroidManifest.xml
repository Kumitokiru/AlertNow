<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.alertnow" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:background="@drawable/alertnow_background"
        android:icon="@drawable/alertnow_round"
        android:label="Alert Now"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/alertnow_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />

        <activity
            android:name=".MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".StarterPage"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginPage" />
        <activity android:name=".ResidentPage" />
        <activity android:name=".SignupPage" />
        <activity android:name=".BarangayPage" />
        <activity android:name=".CDRRMOPage" />
        <activity android:name=".CityHealthPage" />
        <activity android:name=".HospitalsPage" />
        <activity android:name=".PNPPage" />
        <activity android:name=".BFPPage" />
        <activity android:name=".SignUpType" />
        <activity android:name=".Agencyup" />
        <activity android:name=".LoginType" />
        <activity android:name=".AgencyIn" />
        <activity android:name=".PassReset"/>

        <service android:name=".SyncService" />
    </application>

</manifest>